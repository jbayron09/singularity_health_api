#Singularity_health_api api

## Initial setup
1. Be sure of your virtual environment is activated
2. Run `pip install -r requirements.txt`
3. Run `python manage.py migrate`
4. Run `python manage.py createsuperuser`